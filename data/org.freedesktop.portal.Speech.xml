<?xml version="1.0"?>
<!--
  SPDX-License-Identifier: LGPL-2.1-or-later
  SPDX-FileCopyrightText: Copyright © the xdg-desktop-portal contributors
-->

<node name="/" xmlns:doc="http://www.freedesktop.org/dbus/1.0/doc.dtd">
  <!--
      org.freedesktop.portal.Speech:
      @short_description: Portal for local speech model tasks

      Supported use-cases are ``speech.transcribe`` for verbatim source-language
      transcription and ``speech.translate`` for speech-to-English translation.

      Methods returning request handles accept the standard ``handle_token``
      option. CreateSession also accepts ``session_handle_token``. Other option
      keys are reserved. Failed request responses may include ``error`` and
      ``error_name`` strings in the results vardict. Stream payload signals are
      sent only to the caller that owns the request and are correlated by
      ``request_handle``.

      This documentation describes version 4 of this interface.
  -->
  <interface name="org.freedesktop.portal.Speech">
    <!--
        GetUseCaseAvailability:
        @use_case: ``speech.transcribe`` or ``speech.translate``
        @options: Reserved vardict; no keys are currently defined
        @availability: Struct containing availability, code, and human-readable reason

        Returns whether a local profile is assigned and runnable for @use_case.
    -->
    <method name="GetUseCaseAvailability">
      <arg type="s" name="use_case" direction="in"/>
      <annotation name="org.qtproject.QtDBus.QtTypeName.In1" value="QVariantMap"/>
      <arg type="a{sv}" name="options" direction="in"/>
      <arg type="(bss)" name="availability" direction="out"/>
    </method>

    <!--
        CreateSession:
        @parent_window: Identifier for the application window, see :doc:`window-identifiers`
        @use_case: ``speech.transcribe`` for source-language transcription,
          or ``speech.translate`` for speech-to-English translation
        @instructions: Stable system instructions for this session
        @options: Vardict with optional ``handle_token`` and ``session_handle_token`` strings
        @handle: Object path for the :ref:`org.freedesktop.portal.Request` object representing this call

        The session @use_case selects whether StreamTranscribe returns a
        verbatim source-language transcript or an English translation. The
        session handle is returned in the Request response under the
        ``session_handle`` key.
    -->
    <method name="CreateSession">
      <arg type="s" name="parent_window" direction="in"/>
      <arg type="s" name="use_case" direction="in"/>
      <arg type="s" name="instructions" direction="in"/>
      <annotation name="org.qtproject.QtDBus.QtTypeName.In3" value="QVariantMap"/>
      <arg type="a{sv}" name="options" direction="in"/>
      <arg type="o" name="handle" direction="out"/>
    </method>

    <!--
        Prewarm:
        @session_handle: A speech session handle returned by CreateSession
        @options: Vardict with optional ``handle_token`` string
        @handle: Object path for the :ref:`org.freedesktop.portal.Request` object representing this call

        Starts the backing runtime before the first user-visible operation.
        Completion, cancellation, and failures are reported through the
        Request::Response signal.
    -->
    <method name="Prewarm">
      <arg type="o" name="session_handle" direction="in"/>
      <annotation name="org.qtproject.QtDBus.QtTypeName.In1" value="QVariantMap"/>
      <arg type="a{sv}" name="options" direction="in"/>
      <arg type="o" name="handle" direction="out"/>
    </method>

    <!--
        StreamTranscribe:

        Starts streaming transcription or translation segments. In this
        prototype, the @audio argument is base64-encoded 16 kHz mono f32le PCM
        and must fit within the session bus message limit after encoding. This
        string transport is only for small, user-initiated media requests; a
        future fd/document-handle transport requires a separate API revision.
        @source_language_hint is the optional spoken input language hint.
        @options: Reserved vardict with optional ``handle_token`` string
        The method returns a Request handle immediately. Segments are emitted as
        TranscriptionReceived signals; completion, cancellation, and failures
        are reported through the Request::Response signal.
    -->
    <method name="StreamTranscribe">
      <arg type="o" name="session_handle" direction="in"/>
      <arg type="s" name="audio" direction="in"/>
      <arg type="s" name="source_language_hint" direction="in"/>
      <annotation name="org.qtproject.QtDBus.QtTypeName.In3" value="QVariantMap"/>
      <arg type="a{sv}" name="options" direction="in"/>
      <arg type="o" name="handle" direction="out"/>
    </method>

    <signal name="ModelLoading">
      <arg type="o" name="request_handle"/>
      <arg type="o" name="session_handle"/>
      <arg type="s" name="message"/>
    </signal>

    <signal name="TranscriptionReceived">
      <arg type="o" name="request_handle"/>
      <arg type="o" name="session_handle"/>
      <arg type="s" name="text"/>
      <arg type="b" name="done"/>
    </signal>

    <property name="version" type="u" access="read"/>
  </interface>
</node>
